

# 2.3 队列

1. 队列：具有一定操作约束的线性表

   - 插入和删除操作：只能在一端插入，而在另一端删除。

   ->数据插入：入队列（AddQ）

   ->数据删除：出队列（DeleteQ）

   ->先来先服务

   ->先进先出：FIFO

   ![image-20211207171847990](C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20211207171847990.png)

2. ## 队列的顺序存储实现：

   ```c
   #define MaxSize
   struct QNode {
       ElementType Data[MaxSize];
       int rear;
       int front;
   };
   typedef struct QNode *Queue;
   ```

   

![image-20211207172318569](C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20211207172318569.png)

![image-20211207172358413](C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20211207172358413.png)

![image-20211207172520348](C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20211207172520348.png)

```c
Queue CreateQueue( int MaxSize )
{
    Queue Q = (Queue)malloc(sizeof(struct QNode));
    Q->Data = (ElementType *)malloc(MaxSize * sizeof(ElementType));
    Q->Front = Q->Rear = 0;
    Q->MaxSize = MaxSize;
    return Q;
}
```

(1). 入队列：

```c
void AddQ(Queue PtrQ, ElementType item) {
    if((PtrQ->rear + 1) % MaxSize == PtrQ->front) {
        printf("队列满");
        return;
    }
    PtrQ->rear = (PtrQ->rear+1) % MaxSize;
    PtrQ->Date[PtrQ->rear] = item;
}
```

(2). 出队列：

```c
ElementType DeleteQ(Queue PtrQ) {
    if(PtrQ->front == PtrQ->rear) {
        printf("队列空");
        return ERROR;
    } else {
        PtrQ->front = (PtrQ->front+1)%MaxSize;
        return PtrQ->Data[PtrQ->front];
    }
}
```

2. ## 队列的链式存储实现：

   ![image-20211207174407226](C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20211207174407226.png)

```c
struct Node{
    ElementType Data;
    struct Node *next;
};
struct QNode {
    struct Node *rear;
    struct Node *front;
};
typedef struct QNode *Queue;
Queue PtrQ;
```

![image-20211207175041146](C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20211207175041146.png)

![image-20211207175207409](C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20211207175207409.png)

```c
ElementType DeleteQ(Queue PtrQ) {
    struct Node *FrontCell;
    ElementType FrontElem;
    
    if(PtrQ->front == NULL) {
        printf("队列空");
        return ERROR;
    }
    FrontCell = PtrQ -> front;
    if(PtrQ->front == PtrQ->rear)
        PtrQ->front = PtrQ->rear = NULL;
    else
        PtrQ->front = PtrQ->front->Next;
    FrontElem = FrontCell->Data;
    free(FrontCell);
    return FrontElem;
}
```

