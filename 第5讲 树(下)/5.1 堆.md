# 5.1 堆

## 5.1.1 什么是堆（heap）

**优先队列(Priority Queue)**：特殊的“队列”，取出元素的顺序是依照元素的 **优先权（关键字）** 大小，而不是元素进入队列的先后顺序。

![在这里插入图片描述](https://img-blog.csdnimg.cn/20200708092116123.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0lPU1NIQU4=,size_16,color_FFFFFF,t_70)

```
大O记号(上界)
Ω记号(下界)
θ记号（渐近界）
```

![在这里插入图片描述](https://img-blog.csdnimg.cn/2020070809355852.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0lPU1NIQU4=,size_16,color_FFFFFF,t_70)

![在这里插入图片描述](https://img-blog.csdnimg.cn/20200708093717120.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0lPU1NIQU4=,size_16,color_FFFFFF,t_70)

![在这里插入图片描述](https://img-blog.csdnimg.cn/20200708094040886.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0lPU1NIQU4=,size_16,color_FFFFFF,t_70)

```
堆：
首先是一个完全二叉树
然后，每一个结点都比子结点大或者小，大的为最大堆。小的为最小堆
特性：从根节点，到下面某一个节点的路径，是一个有序的序列
```

![image-20220110101239406](C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20220110101239406.png)

## 5.1.2 最大堆操作

```c
typedef struct HeapStruct *MaxHeap;
struct HeapStruct {
    ElementType *Elements; //存储元素的数组
    int Size; //堆中当前元素个数
    int Capacity;//堆的最大容量
};
typedef Heap MaxHeap; /* 最大堆 */
typedef Heap MinHeap; /* 最小堆 */
#define MaxData 1000  /* 该值应根据具体情况定义为大于堆中所有可能元素的值 */
```

```c
MaxHeap Create(int MaxSize) {
    //创建容量为MaxSize的空的最大堆
    MaxHeap H = malloc(sizeof(struct HeapStruct));
    H->Elements = malloc((MaxSize+1) * sizeof(ElementType));
    H->Size = 0;
    H->Capacity = MaxSize;
    H->Elements[0] = MaxData; //定义“哨兵”为大于堆中所有可能的元素的值，便于以后更快的操作
}
```

 **算法：将新增的结点插入到其父节点到根节点的有序序列中**

```c
bool IsFull(MaxHeap H) {
    return (H->Size == H->Capacity);
}

void Insert(MaxHeap H, ElementType item) {
    //将元素item插入最大堆H，其中H->Elements[0]已经定义为哨兵
    int i;
    if(isFull(H)) {
        printf("最大堆已满");
        return;
    } 
    i = ++H->Size;//i指向插入后堆中的最后一个元素的位置
    for( ; H->ElementType[i/2] < item; i /= 2) {
        H->Elements[i] = Elements[i/2];//向下过滤结点
    }
    H->Elements[i] = item;//将item插入
}
```

**最大堆的删除**

取出根节点（最大值）元素，同时删除堆的一个结点。

```c
bool IsEmpty(MaxHeap H) {
    return (H->Size == 0);
}

ElementType DeleteMax(MaxHeap H) {
    int Parent, Child;
    ElementType MaxItem, temp;
    if(IsEmpty(H)) {
        printf("最大堆为空");
        return;
    }
    MaxItem = H->Elements[1]; //取出根节点最大值
    //用最大堆中最后一个元素从根结点开始向上过滤下层结点
    temp = H->Elements[H->Size--];
    for(Parent = 1; Parent*2 <= H->Size; Parent = Child) {
        Child = Parent*2;//首先假定左儿子最大
        //判断最大到底是左儿子还是右儿子
        if((Child != H->Size) && (H->Elements[Child] < H->Elements[Child+1])) 
            Child++;//Child指向左右儿子结点的较大者
        if(temp >= H->Elements[Child]) break;
        else //移动temp到下一层
            H->Elements[Parent] = H->Elements[Child];
    }
    H->Elements[Parent] = temp;
    return MaxItem;
}
```

**最大堆的建立**

堆的一个应用：堆排序 —— 需要先建立堆

```c
void PercDown(MaxHeap H, int p) {
    //将H中以H->Elements[p]为根的子堆调整为最大堆
    int Parent Child;
    ElementType X;
    
    X = H->Elements[p]; //取出根节点存放的值
    for(Parent = p; Parent*2 <= H->Size; Parent = Child) {
        Child = Parent*2;
        if((Child != H->Size) && (H->Elements[Child] < H->Elements[Child++])) 
            Child++;//找到Child合适的位置
        if(X >= H->Elements[Child]) break;
        else 
            H->Elements[Parent] = H->Elements[Child];
    }
    H->Elements[Parent] = X;
}
```

```c
void BuildHeap(MaxHeap H) {
    //调整H->Elements[]中的元素，使满足最大堆的有序性
    //这里假设所有H->Size个元素已经存在于H->Elements[]中
    int i;
    //从最后一个结点的父节点开始，到根节点1（从下往上）
    for(i = H->Size; i > 0; i--) {
        PercDown(H, i);
    }
}
```

