# 6.2 图的遍历

## 深度优先搜索(Depth-First-Search)

类似于树的**先序遍历**，它沿着树的深度，遍历树的结点，极可能深的搜索树的分支，当结点V的所有边都已被探寻过，搜索将回溯到发现结点V的那边的起始结点，假设有如下二叉树

```c
      A          
    /   \         
   B     C     
  / \   / \              
 D   E F   G 
```

A是第一个访问的，然后顺序是B,D，然后是E，接着再是C F G。

若有N个顶点，E条边，时间复杂度是：

- 用邻接表存储图，有O(N+E)
- 用邻接矩阵存储图，有O(N^2)

```c
/* 邻接表存储的图 - DFS */
void Visit(Vextex V) {
    printf("正在访问顶点%d\n", V);
}

/* Visited[]为全局变量，已经初始化为false */
void DFS(LGraph Graph, Vertex V, void (*Visit)(Vertex)) {
    /* 以V为出发点对邻接表存储的图Graph进行DFS搜索 */
    PtrToAdjVNode W;
    
    Visit(V);/* 访问第V个顶点 */
    Visit[V] = true;/* 标记V已访问 */
    
    for(W = Graph->G[V].FirstEdge; W; W = W->Next) {
        /* 对V的每个邻接点W->AdjV */
        if(!Visit[W->Adjv])/* 若W->AdjV未被访问 */
            DFS(Graph, W->Adjv, Visit);/* 则递归访问 */
    }
}
```

## 广度优先搜索(Breadth-First-Search)

类似于树的**层次遍历**，从根节点开始，沿着树的宽度遍历树的节点。如果所有节点均被访问，则算法中止。

```C
      A          
    /   \         
   B     C     
  / \   / \              
 D   E F   G 
```

这里A是第一个访问的，然后顺序是 B、C，然后再是 D、E、F、G。可以借助队列结构实现BFS访问。

若有N个顶点，E条边，时间复杂度是

- 用邻接表存储，O(N+E)
- 用邻接矩阵存储，O(N^2)

```c
//队列声明
#define MaxSize
struct QNode {
    ElementType Data[MaxSize];
    int Rear;
    int Front;
    int MaxSize;
};
typedef struct QNode *Queue;

/* 邻接矩阵存储的图 - BFS */
//此函数根据图的不同类型要做不同的实现，关键取决于对不存在的边的表示方法。
// 例如对有权图, 如果不存在的边被初始化为INFINITY, 则函数实现如下: 
//INFINITY为自己定义的数字
bool IsEdge( MGraph Graph, Vertex V, Vertex W )
{
    return Graph->G[V][W]<INFINITY ? true : false;
}

//创建队列
Queue CreateQueue(int MaxSize) {
    Queue Q = (Queue)malloc(sizeof(struct QNode));
    Q->Data = (ElementType *)malloc(MaxSize*sizeof(ElementType));
    Q->Front = Q->Rear = 0;
    Q->MaxSize = MaxSize;
    return Q;
}

//入队列
void AddQ(Queue PtrQ, ElementType item) {
    if((PtrQ->Rear+1) % MaxSize == PtrQ->Front) {
        printf("队列满");
        return;
    }
    PtrQ->Rear = (PtrQ->Rear+1) % MaxSize;
    PtrQ->Data[PtrQ->Rear] = item;
}

//出队列
ElementType DeleteQ(Queue PtrQ) {
    if(PtrQ->Front == PtrQ->Rear) {
        printf("队列空");
        return -1;
    } else {
        PtrQ->Front = (PtrQ->Front+1) % MaxSize;
        return PtrQ->Data[PtrQ->Front];
    }
}

/* Visited[]为全局变量，已经初始化为false */
void BFS(MGraph Graph, Vertex S, void(*Visit)(Vertex)) {
    Queue Q;
    Vertex V, W;
    Q = CreateQ(MaxSize);// 创建空队列, MaxSize为外部定义的常数 
    /* 访问顶点S：此处可根据具体访问需要改写 */
    Visit(S);
    Visited[S] = true;/* 标记S已访问 */
    AddQ(Q, S);/* S入队列 */
    
    while(Q->Front != Q->Rear) {
        V = DeleteQ(Q); /* 弹出V */
        for(W = 0; W < Graph->Nv; W++)/* 对图中的每个顶点W */
            if(!Visited[W] && IsEdge(Graph, V, W)) {
                /* 访问顶点W */
                Visit(W);
                Visited[W] = true;/* 标记W已访问 */
                AddQ(Q, W);/* W入队列 */
            }
    }
    
}
```

## 如果图不连通怎么办？

**连通**：如果从v到w存在一条(无向)路径，则称v和w是连通的。

**路径**：v到w的路径是一系列顶点{V, v1 , v2 , ... , vn , W}的集合，其中任一对相邻的顶点间都有图中的边，**路径的长度**是路径中的边数(如果带权，则是所有边的权重和)。如果v到w之间的所有顶点都不同，则称**简单路径**。

**回路**：起点等于终点的路径

**连通图**：途中任意两顶点均连通。

**连通分量**：无向图的极大连通子图

- 极大顶点数：再加1个顶点就不连通了
- 极大边数：包含子图中所有顶点相连的所有边。

![img](https://i.loli.net/2020/03/28/1n72EBdpWwhaOrM.png)

**强连通**：有向图中顶点v和w之间存在双向路径，则称v和w是强连通的

**强连通图**：有向图中任意两顶点均强连通

**强连通分量**：有向图的极大强连通子图