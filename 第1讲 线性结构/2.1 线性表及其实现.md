# 线性表及其实现

# 1.多项式的表示

<img src="C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20211202172146093.png" alt="image-20211202172146093" style="zoom:50%;" />

<img src="C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20211202172329504.png" alt="image-20211202172329504" style="zoom: 50%;" />

<img src="C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20211202172629532.png" alt="image-20211202172629532" style="zoom:50%;" />

##  <img src="C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20211202183606574.png" alt="image-20211202183606574" style="zoom:50%;" />

## 线性表的存储：

声明：

```c
typedef int Position;
typedef struct LNode* List;
struct LNode {
    ElementType Data[MAXSIZE];
    Position Last;
};
```

## 一、线性存储：

1. 初始化（ 建立空的顺序表 ）：

   ```c
   List MakeEmpty()
   {
       List L;
   
       L = (List)malloc(sizeof(struct LNode));
       L->Last = -1;
   
       return L;
   }
   
   ```

2. 查找:

   ```c
   #define ERROR -1
   
   Position Find( List L, ElementType X )
   {
       Position i = 0;
   
       while( i <= L->Last && L->Data[i] != X )
           i++;
       if ( i > L->Last )  return ERROR; /* 如果没找到，返回错误信息 */
       else  return i;  /* 找到后返回的是存储位置 */
   }
   ```

   3.插入：

   ```c
   bool Insert( List L, ElementType X, Position P ) 
   { /* 在L的指定位置P前插入一个新元素X */
       Position i;
   
       if ( L->Last == MAXSIZE-1) {
           /* 表空间已满，不能插入 */
           printf("表满"); 
           return false; 
       }  
       if ( P<0 || P>L->Last+1 ) { /* 检查插入位置的合法性 */
           printf("位置不合法");
           return false; 
       } 
       for( i=L->Last; i>=P; i-- )
           L->Data[i+1] = L->Data[i]; /* 将位置P及以后的元素顺序向后移动 */
       L->Data[P] = X;  /* 新元素插入 */
       L->Last++;       /* Last仍指向最后元素 */
       return true; 
   } 
   
   ```
   
   

4.删除：

```c
bool Delete( List L, Position P )
{ /* 从L中删除指定位置P的元素 */
    Position i;

    if( P<0 || P>L->Last ) { /* 检查空表及删除位置的合法性 */
        printf("位置%d不存在元素", P ); 
        return false; 
    }
    for( i=P+1; i<=L->Last; i++ )
        L->Data[i-1] = L->Data[i]; /* 将位置P+1及以后的元素顺序向前移动 */
    L->Last--; /* Last仍指向最后元素 */
    return true;   
}
```

## 二、链式存储：

1.求表长：

```c
typedef int Position;
typedef struct LNode *List;
struct LNode {
    ElementType Data;
    List next;
};
int Length(List Ptrl) {
    List p = Ptrl;
    int j = 0;
    while( p ) {
        p = p->next;
        j ++;
    }
    return j;
}
```

2.查找

（1）按序号查找：FindKth

范围序号位置所代表的数值

```c
List FindKth(int k, List Ptrl) {
    List p = Ptrl;
    int i = 1;
    while(p->next != NULL && i < k) {
        i ++;
    }
    if(p->next == NULL) {
        return NULL
    } else {
        return p;
    }
}
```

（2）按值查找:Find

返回对应值代表的序号位置

```c
List Find(ElementType X, List Ptrl) {
    List p = Ptrl;
    while(p != NULL && p->Date != X) {
        p = p->Next;
    }
    return p;
}
```

（3）插入（在第i-1（1 <= i <= n+1）个结点后插入一个值为X的新结点）

![image-20211203173304439](C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20211203173304439.png)

```c
List Insert(ElementType X, int i, List Ptrl) {
    List p,s;
    if(i == 1) {
        s = (List)malloc(sizeof(struct LNode));
        s->Date = X;
        s->Next = Ptrl;
        return s;
    }
    p = FindKth(i-1, Ptrl);
    if(p == NULL) {
        printf("error");
        return NULL;
    }
    else
    {
        s = (List)malloc(sizeof(struct LNode));
        s->Date = X;
        s->Next = p->Next;
        p->Next = s;
        return Ptrl;
    }
}
```

4.删除（删除链表的第 i（1 <= i <= n）个位置上的结点）

![image-20211203173821365](C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20211203173821365.png)

```c
List Delete(int i, List Ptrl) {
    List p, s;
    if(i == 1) {
        s = Ptrl;
        if(Ptrl != NULL) 
            Ptrl = Ptrl->Next;
        else
            return NULL;
        free(s);
        return Ptrl;
    }
    p = FindKth(i-1, Ptrl)
        if(p == NULL) {
            printf("error");
        } else if(p->Next == NULL) {
            printf("error");
        } else {
            s = p->Next;
            p->Next = s->Next;
            free(s);
            return Ptrl;
        }
}
```

## 王道考研版本

```c++
#include <bits/stdc++.h>
using namespace std;
typedef struct LNode {
	int data;
	struct LNode* next;
} LNode, *LinkList;
LinkList List_HeadInsert(LinkList &L) {
	LNode *s;
	int x;
	L = (LinkList)malloc(sizeof(LNode)); // 建立头节点 
	L->next = NULL; // 初始化为NULL 
	scanf("%d", &x);
	while(x != 99999) {
		s = (LNode*)malloc(sizeof(LNode));
		s->data = x;
		s->next = L->next;
		L->next = s;
		scanf("%d", &x);
	}
	return L;
}
LinkList List_EndInsert(LinkList &L) {
	int x;
	L = (LinkList)malloc(sizeof(LNode)); // 建立头节点  
	LNode *s, *r;
	*r = L; // r指针指向链表尾部 
	scanf("%d", &x);
	while(x != 99999) {
		s = (LNode*)malloc(sizeof(LNode));
		s->data = x;
		r->next = s;
		r = s;
		scanf("%d", &x);
	} 
	return L;
}
// 按序号查找结点
LNode* GetElem(ListLink L, int i) {
	int j = 1;
	if(i == 0) return L; // 若i等于0，则返回头节点
	if(i < 1) return NULL; 
	LNode *q = L->next; // 把第一个结点给q，从第一个结点开始找
	while(q != NULL && j < i) {
		q = q->next;
		j++;
	} 
	return q;
}
// 按值查找表中结点
LNode* LocateElem(ListLink L, int e) {
	LNode *p = L->next;
	while(p != NULL && p->data != e) {
		p = p->next;
	}
	return p;
}
// 插入节点操作(将值为e的结点插入到i位置上)
bool InsertElem(ListLink &L, int i, int e) {
	if(i < 1) return false;
	LNode *q = GetELem(L, i-1); // 找到前驱节点
	if(q == NULL) return false;
	Lnode *s;
	s = (LNode*)malloc(sizeof(LNode));
	s->data = e;
	s->next = q->next;
	q->next = s;
	return true;
}
// 拓展：对某一结点进行前插操作(在p结点的前面插入一个结点e)
bool InsertPriorNode(LNode *p, int e) {
	if(p == NULL) return false;
	LNode *s = (LNode*)malloc(sizeof(LNode));
	if(s == NULL) return false; // 有时候可能内存分布失败（没内存了）
	s->next = p->next;
	p->next = s;
	s->data = p->data;
	p->data = e;
	return true; 	
} 
// 删除节点的操作
bool DeleteNode(ListLink &L, int i, int e) {
	if(i < 1) return false;
	LNode *p;
	p = GetElem(L, i-1);
	if(p == NULL) return false;
	LNode *q = p->next;
	p->next = q->next;
	free(q);
	return true;
}
// 删除指定节点*p
bool DeleteElem(LNode *p) {
	if(p == NULL) return false;
	LNode *q = p->next;
	p->data = q->data;
	p->next = q->next;
	free(q);
	return true; 
} 
// 求表长
int Length(ListLink L) {
	int count = 0;
	LNode *p = L->next;
	while(p != NULL) {
		p = p->next;
		count++;
	}
	return count;
}
```

