# 5.2 哈夫曼树与哈夫曼编码

##   5.2.1 哈夫曼树的构造

### 什么是哈夫曼树？

当用 n 个结点（都做叶子结点且都有各自的权值）试图构建一棵树时，如果构建的这棵树的带权路径长度最小，称这棵树为“最优二叉树”，有时也叫“赫夫曼树”或者“哈夫曼树”。

在构建哈弗曼树时，要使树的带权路径长度最小，只需要遵循一个原则，那就是：权重越大的结点离树根越近。在图 1 中，因为结点 a 的权值最大，所以理应直接作为根结点的孩子结点。

![img](http://c.biancheng.net/uploads/allimg/190427/09563Tb0-0.png)

```c
typedef strcut TreeNode* HuffumanTree;
struct TreeNode {
    int Weight;
    HuffmanTree Left, Right;
}
HuffmanTree Huffman(MinHeap H) {
    //假设H->Size个权值已经存在H->Elements[]->Weight里
    int i; HuffmanTree T;
    BuildMinHeap(H);//将H->Elements[]按权值调整为最小堆
    for(i = 1; i < H->Size; i++) { //做H->Size-1次合并
        T = malloc(sizeof(struct TreeNode));
        T->Left = DeleteMin(H);//从最小堆中删除一个结点，作为新T的左子节点
        T->Right = DeleteMin(H);//从最小堆中删除一个结点，作为新T的右子节点
        T->Weight = T->Left->Weight + T->Right->Weight;
        Insert(H, T);//将新T插入最小堆
    }
    T = DeleteMin(H);
    return T;
}
```

**最小堆的删除**

```c

bool IsEmpty(MinHeap H) {
    return (H->Size == 0);
}

ElementType DeleteMin(Minheap H){
    int Parent, Child;
    ElelmentType MinItem, temp;
    if(IsEmpty) {
        printf("最小堆为空");
        return;
    }
    MinItem = H->Elements[1];//取出根节点最小值
    temp = H->Elements[Size--];//用最小堆最后一个元素从根节点开始向上过滤下层结点
    for(Parent = 1; Parent*2 <= H->Size; Parent = Child) {
        Child = Parent*2;//假设左儿子最小
        if((Child*2 != H->Size) && (H->Elements[Child] > H->Elements[Child++]))
            Child++;//Child指向左右儿子结点的较小者;
        if(temp < H->Elements[Child]) break;
        else 
            H->Elements[Parent] = H->Elements[Child];
    }
    H->Elements[Parent] = temp;
    return MinItem;
}
```

**最小堆的建立**

```c
void PercDown(MinHeap H, int p) {
    //将H中以H->Elements[p]为根的子堆调整为最大堆
    int Parent Child;
    ElementType X;
    
    X = H->Elements[p]; //取出根节点存放的值
    for(Parent = p; Parent*2 <= H->Size; Parent = Child) {
        Child = Parent*2;
        if((Child != H->Size) && (H->Elements[Child] > H->Elements[Child++])) 
            Child++;//找到Child合适的位置
        if(X < H->Elements[Child]) break;
        else 
            H->Elements[Parent] = H->Elements[Child];
    }
    H->Elements[Parent] = X;
}

void BuildHeap(MinHeap H) {
    //调整H->Elements[]中的元素，使满足最大堆的有序性
    //这里假设所有H->Size个元素已经存在于H->Elements[]中
    int i;
    //从最后一个结点的父节点开始，到根节点1（从下往上）
    for(i = H->Size; i > 0; i--) {
        PercDown(H, i);
    }
}
```



### 哈夫曼树的特点：

- 没有度为1的结点；
- n个叶子节点的哈夫曼树共有2n-1个结点；
- 哈夫曼树的任意非叶节点的左右子树交换后仍然是哈夫曼树；
- 对于同一组权值，存在不同构的两颗哈夫曼树

## 5.2.2 哈夫曼编码

![image-20220110160418563](C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20220110160418563.png)

问题：怎么进行不等长编码？

​			如何避免二义性？

### 二叉树用于编码

==所有的字符都在二叉树的叶结点上==

![image-20220110160717871](C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20220110160717871.png)

![image-20220110161509657](C:\Users\44174\AppData\Roaming\Typora\typora-user-images\image-20220110161509657.png)

